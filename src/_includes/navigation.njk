<nav class="nav nav--primary" role="navigation" tabindex="-1">
    {# Nav Links  #}
    <ul class="nav__menu">
        <li class="nav__item">
            {% set homeUrl %}/{{page.lang}}/{% endset %}
            <a class="nav__link" href="{{homeUrl | url}}">Home</a>
        </li>
        <li class="nav__item">
            {% set aboutUrl %}/{{page.lang}}/about/{% endset %}
            <a class="nav__link" href="{{aboutUrl | url}}">About</a>
        </li>
        <li class="nav__item">
            {% set notesUrl %}/{{page.lang}}/notes/{% endset %}
            <a class="nav__link" href="{{notesUrl | url}}">Notes</a>
        </li>
{#         
        {%- for item in collections.all | eleventyNavigation -%}
            {%- if item.url -%}
                {%- set isCurrent = (item.url == page.url) -%}
                {%- set isParent = item.url != '/' and item.url in page.url -%}
                <li class="nav__item{% if isCurrent or isParent %} nav__item--active{% endif %}">
                    <a class="nav__link" href="{% if isCurrent %}#main{% else %}{{ item.url | url }}{% endif %}" {% if isCurrent %} aria-current="page" {% endif %}>
                        {{ item.title }}
                    </a>
                </li>
            {%- endif -%}
        {%- endfor -%} #}
    </ul>
</nav>
