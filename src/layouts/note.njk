---
layout: base
image:
---
<article class="container" data-pagefind-body>
  <h1>{{ title }}</h1>

  {% if (page.fileSlug !== "timeline") %}
  <p>
    <span class="meta__stats">{% if page.lang == 'en' %}About{% else %}Circa{% endif %} {{ content | readingTime }} {%
      if page.lang == 'en' %}reading time{% else %}per la lettura{% endif %}</span>
  </p>
  {% endif %}

  {{ content | safe }}

  {% if (page.fileSlug !== "timeline") %}
  <div class="note-metadata" data-pagefind-ignore>
    <time class="notelist-date" datetime="{{ page.date | dateToFormat('yyyy-MM-dd') }}">{{ page.date | dateToFormat('dd
      LLL yyyy') }}</time>
    / Tags: {% for tag in tags | filterTagList %}
    {% set tagUrl %}/{{ page.lang }}/tags/{{ tag | slug }}/{% endset %}
    <a href="{{ tagUrl | url }}" class="note-tag">{{ tag }}</a>
    {% endfor %}
  </div>
  {% endif %}

  {# {%- if collections.notes %}
  {%- set previousnote = collections.notes | getPreviousCollectionItem %}
  {%- set nextnote = collections.notes | getNextCollectionItem %}
  {%- if nextnote or previousnote %}
  <ul class="links-nextprev" data-pagefind-ignore>
    {%- if previousnote %}
    <li>
      {%- if page.lang == 'it' %}Precedente{% else %}Previous{% endif %}: <a href="{{ previousnote.url | url }}">{{
        previousnote.data.title }}</a>
    </li>
    {% endif %}
    {%- if nextnote %}
    <li>
      {%- if page.lang == 'it' %}Successiva{% else %}Next{% endif %}: <a href="{{ nextnote.url | url }}">{{
        nextnote.data.title }}</a>
    </li>
    {% endif %}
  </ul>
  {%- endif %}
  {%- endif %} #}
</article>