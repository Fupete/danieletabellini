diff --git a/node_modules/speedlify-score/speedlify-score.js b/node_modules/speedlify-score/speedlify-score.js
index 907ffcd..f0cc1df 100644
--- a/node_modules/speedlify-score/speedlify-score.js
+++ b/node_modules/speedlify-score/speedlify-score.js
@@ -82,15 +82,15 @@ class SpeedlifyScore extends HTMLElement {
 	color: var(--_circleText, #666);
 }
 .circle-good {
-	color: var(--_circleText, #088645);
+	color: var(--_circleText, #088846);
 	border-color: var(--_circleBorder, #0cce6b);
 }
 .circle-ok {
-	color: var(--_circleText, #ffa400);
+	color: var(--_circleText, #A56900);
 	border-color: var(--_circleBorder, #ffa400);
 }
 .circle-bad {
-	color: var(--_circleText, #ff4e42);
+	color: var(--_circleText, #EC0D00);
 	border-color: var(--_circleBorder, #ff4e42);
 }
 .meta {
@@ -108,13 +108,13 @@ class SpeedlifyScore extends HTMLElement {
 	line-height: 1;
 }
 .rank-change.up {
-	color: green;
+	color: #088846;
 }
 .rank-change.up:before {
 	content: "⬆";
 }
 .rank-change.down {
-	color: red;
+	color: #EC0D00;
 }
 .rank-change.down:before {
 	content: "⬇";
@@ -162,7 +162,7 @@ class SpeedlifyScore extends HTMLElement {
 	async init() {
 		if(this.rawData) {
 			let data = JSON.parse(this.rawData);
-			this.setDateAttributes(data);
+			// this.setDateAttributes(data);
 			this._initTemplate(data);
 			return;
 		}
@@ -189,18 +189,18 @@ class SpeedlifyScore extends HTMLElement {
 		}
 
 		let data = await urlStore.fetchData(this.speedlifyUrl, hash);
-		this.setDateAttributes(data);
+		// this.setDateAttributes(data);
 
 		this._initTemplate(data, forceRerender);
 	}
 
-	setDateAttributes(data) {
-		if(!("Intl" in window) || !Intl.DateTimeFormat || !data.timestamp) {
-			return;
-		}
-		const date = new Intl.DateTimeFormat().format(new Date(data.timestamp));
-		this.setAttribute("title", `Results from ${date}`);
-	}
+	// setDateAttributes(data) {
+	// 	if(!("Intl" in window) || !Intl.DateTimeFormat || !data.timestamp) {
+	// 		return;
+	// 	}
+	// 	const date = new Intl.DateTimeFormat().format(new Date(data.timestamp));
+	// 	this.setAttribute("title", `Results from ${date}`);
+	// }
 
 	getScoreClass(score) {
 		if(score === "" || score === undefined) {
